server:
  port: 8082

spring:
  application:
    name: document-service
  datasource:
    url: jdbc:postgresql://localhost:5434/db_documents
    username: docuupload
    password: docuupload
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    mongodb:
      uri: mongodb://docuupload:docuupload@localhost:27017/db_documents?authSource=admin
  rabbitmq:
    host: localhost
    port: 5672
    username: docuupload
    password: docuupload

jwt:
  secret: docu-upload-super-secret-key-che-deve-essere-lunga-almeno-256-bit-ok
  expiration-ms: 86400000

influxdb:
  enabled: true
  url: http://localhost:8086
  token: docuupload-influxdb-token
  org: docuupload
  bucket: performance
